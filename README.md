# public-dns

Lists of Domain-Names and IP addresses of known Public DNS Services.

The Domain/HOSTS-lists only contains resolvable domain-names (and resulting IP-Addresses).

The IP/CIDR lists may contain inactive IP-Addresses.

All Domain lists are domain-sorted from root/tope-level-domain down to the sub-domain.

